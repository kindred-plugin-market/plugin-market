# 硬件对比（Hardware）规划功能

> 本文件记录 hardware 模块**未实现 / 待验证**的功能规划，与 `../product-specs/hardware.md` 同结构。
> 实现一项即从本文件移除，并同步到产品说明；规划新增功能先写到这里再开发。

## 待实现

- [ ] **可追溯的价格数据源 + stale 状态**：当前价格为内置快照；需接入可追溯价格数据源，并定义「数据更新时间」与过期（stale）状态展示。
- [ ] **导出对比为图片 / PDF**：覆盖长表格分页与中英文两套语言。

## 待验证

- [ ] 真机验证各品类数据模块在 macOS 上的渲染：14 个 Tab 动态加载、矩阵最优值高亮、范围条、参考链接打开外部浏览器。
- [ ] 多型号选择（10+ 列）时 sticky 首列 + 横向滚动在窄窗口下的可用性。

## 远期

- [ ] 全局路线图（`docs/ROADMAP.md`）未给 hardware 单独 R 编号段；仅 R00 发布契约约束其为 macOS-only。若后续放开平台或接入联网数据源需在此登记。
- [ ] 型号库扩充策略（新增品类/新增型号的维护流程）。

## 变更记录

> 每轮功能改动先在此追加一行，再在实施后同步进产品说明。

- 2026-09-03：生成产品说明文档与规划文档（对齐 photo-triage 模板）。
